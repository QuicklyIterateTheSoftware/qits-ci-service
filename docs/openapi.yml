# THIS FILE IS GENERATED BY OpenApiSchemaExportTest — DO NOT EDIT MANUALLY.
# Run `./mvnw -pl service -am test -Dtest=OpenApiSchemaExportTest -Dsurefire.failIfNoSpecifiedTests=false` to regenerate.
---
openapi: 3.1.0
components:
  schemas:
    Blocker:
      type: object
      properties:
        runId:
          type: string
        repoName:
          type: string
    CancelReleaseRequestRunsRequest:
      type: object
      required:
      - repoId
      - releaseRequestId
      properties:
        repoId:
          type: string
          description: The repository whose runs to stop
        releaseRequestId:
          type: string
          description: The release request whose work is withdrawn
    CancelReleaseRequestRunsResponse:
      type: object
      properties:
        runIds:
          type: array
          items:
            type: string
    CancelRunRequest:
      type: object
      properties:
        reason:
          type: string
    CiLiveStepDto:
      type: object
      properties:
        stepIndex:
          type: integer
          format: int32
        startedAt:
          $ref: "#/components/schemas/Instant"
        output:
          type: string
    CiRunDto:
      type: object
      properties:
        id:
          type: string
        repoId:
          type: string
        projectId:
          type: string
        repoName:
          type: string
        branch:
          type: string
        commitSha:
          type: string
        status:
          $ref: "#/components/schemas/CiRunStatus"
        createdAt:
          $ref: "#/components/schemas/Instant"
        startedAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
        cancellationReason:
          type: string
        supersededByRunId:
          type: string
        daemonVersion:
          type: string
        triggerType:
          $ref: "#/components/schemas/CiTriggerType"
        triggerEventId:
          type: string
        triggerEventName:
          type: string
        releaseRequestId:
          type: string
        retryOfRunId:
          type: string
        configPath:
          type: string
        priority:
          type: string
        expectedStepDurationsMillis:
          type: array
          items:
            type: integer
            format: int64
        steps:
          type: array
          items:
            $ref: "#/components/schemas/CiStepDto"
        live:
          $ref: "#/components/schemas/CiLiveStepDto"
        phase:
          $ref: "#/components/schemas/CiRunPhase"
        queuePosition:
          type: integer
          format: int32
        expectedStartInMillis:
          type: integer
          format: int64
        expectedFinishInMillis:
          type: integer
          format: int64
        predictionUnavailable:
          type: string
        ordering:
          $ref: "#/components/schemas/CiRunOrderingDto"
    CiRunOrderingDto:
      type: object
      properties:
        position:
          type: integer
          format: int32
        kindTier:
          type: integer
          format: int32
        priority:
          type: string
        priorityRank:
          type: integer
          format: int32
        topologyBlockers:
          type: array
          items:
            $ref: "#/components/schemas/Blocker"
        selection:
          $ref: "#/components/schemas/Selection"
    CiRunPhase:
      type: string
      enum:
      - RELEASE_REQUEST
      - RELEASE
    CiRunStatus:
      type: string
      enum:
      - QUEUED
      - RUNNING
      - SUCCESS
      - FAILED
      - CANCELLED
      - CONFIG_ERROR
      - TIMED_OUT
    CiStepDto:
      type: object
      properties:
        stepIndex:
          type: integer
          format: int32
        image:
          type: string
        status:
          $ref: "#/components/schemas/CiStepStatus"
        exitCode:
          type: integer
          format: int32
        startedAt:
          $ref: "#/components/schemas/Instant"
        finishedAt:
          $ref: "#/components/schemas/Instant"
        output:
          type: string
    CiStepStatus:
      type: string
      enum:
      - PENDING
      - RUNNING
      - SUCCESS
      - FAILED
      - SKIPPED
      - TIMED_OUT
    CiTriggerType:
      type: string
      enum:
      - POST_RECEIVE
      - EVENT
    DaemonPinDto:
      type: object
      properties:
        daemonName:
          type: string
        daemonVersion:
          type: string
        previousDaemonVersion:
          type: string
        source:
          type: string
    Instant:
      type: string
      format: date-time
      examples:
      - 2022-03-10T16:15:50Z
    ListRepositoryIdsResponse:
      type: object
      properties:
        repositoryIds:
          type: array
          items:
            type: string
    ListRepositorySummariesResponse:
      type: object
      properties:
        repositories:
          type: array
          items:
            $ref: "#/components/schemas/RepositorySummaryDto"
    ListRunsResponse:
      type: object
      properties:
        runs:
          type: array
          items:
            $ref: "#/components/schemas/CiRunDto"
    QueueResponse:
      type: object
      properties:
        concurrentBuilds:
          type: integer
          format: int32
        generatedAt:
          $ref: "#/components/schemas/Instant"
        running:
          type: array
          items:
            $ref: "#/components/schemas/CiRunDto"
        queued:
          type: array
          items:
            $ref: "#/components/schemas/CiRunDto"
    ReleasePhaseResponse:
      type: object
      properties:
        repositoryId:
          type: string
        rev:
          type: string
        declared:
          type: boolean
        detail:
          type: string
    RepositorySummaryDto:
      type: object
      properties:
        repositoryId:
          type: string
        projectId:
          type: string
        repoName:
          type: string
        lastRun:
          $ref: "#/components/schemas/CiRunDto"
        lastMainRun:
          $ref: "#/components/schemas/CiRunDto"
    RerunReleaseRequestPhaseRequest:
      type: object
      required:
      - repoId
      - releaseRequestId
      - phase
      properties:
        repoId:
          type: string
          description: The repository whose run to re-fire
        releaseRequestId:
          type: string
          description: The release request the run serves
        phase:
          type: string
          enum:
          - RELEASE_REQUEST
          - RELEASE
          description: "Which phase of the release to re-fire: RELEASE_REQUEST (QA)\
            \ or RELEASE (publish)"
    RetryRunResponse:
      type: object
      properties:
        runId:
          type: string
    Selection:
      type: string
      enum:
      - UNBLOCKED
      - CYCLE_DEGRADED
    TriggerEventRequest:
      type: object
      properties:
        name:
          type: string
          examples:
          - SoftwareRelease
          description: "The event signature, matched against a trigger file's `event:`"
        payload:
          type: object
          description: "The event payload, exactly as it would arrive off the bus"
        occurredAt:
          type: string
          examples:
          - 2026-08-04T09:00:00Z
          description: "ISO-8601 instant. Absent: now."
        eventId:
          type: string
          description: "UUID. Absent: a fresh random one — see the dedupe note."
    TriggerEventResult:
      type: object
      properties:
        eventId:
          type: string
        runIds:
          type: array
          items:
            type: string
        repositoriesRead:
          type: integer
          format: int32
        repositoriesSkipped:
          type: array
          items:
            type: string
  securitySchemes:
    SecurityScheme:
      type: openIdConnect
      openIdConnectUrl: http://qits-platform-idp:8080/idp/.well-known/openid-configuration
      description: Authentication
paths:
  /ci/api/daemon:
    get:
      summary: The daemon binary this instance is configured to launch
      responses:
        "200":
          description: "The pinned daemon binary. daemonVersion is the version of\
            \ the protocol dependency this service is built against, with source \"\
            pinned\"; source is \"override\" when a deployment has set qits.ci.daemon-version-override.\
            \ previousDaemonVersion is always blank: a pin has no fallback rung."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DaemonPinDto"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Daemon Controller
      security:
      - SecurityScheme:
        - qits:system
        - qits:agent
  /ci/api/events/trigger:
    post:
      summary: Run every event pipeline that selects a caller-supplied domain event
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriggerEventRequest"
        required: true
      responses:
        "200":
          description: "Evaluated, with the runs it recorded and which repositories\
            \ it could ask"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerEventResult"
        "400":
          description: "Blank name, missing payload, or an unparseable timestamp or\
            \ id"
        "403":
          description: The token covers no repository here — its project claim names
            a project this instance can place no repository in
        "503":
          description: Not evaluated — no candidate repository could be read. Retry.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerEventResult"
        "401":
          description: Not Authorized
      tags:
      - Ci Event Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /ci/api/repositories:
    get:
      summary: The repository ids qits-ci has recorded runs for
      responses:
        "200":
          description: "Distinct repository ids, ascending"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRepositoryIdsResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/repositories/summary:
    get:
      summary: Each repository with its newest run and its newest run on main
      responses:
        "200":
          description: "One entry per repository, ascending by id"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRepositorySummariesResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/repositories/{repoId}/release-phase:
    get:
      summary: Whether a rev's composed release cycle declares a release phase
      parameters:
      - name: repoId
        in: path
        required: true
        schema:
          type: string
      - description: "A git rev the host can resolve, in practice refs/tags/<version>"
        required: true
        name: rev
        in: query
        schema:
          type: string
      responses:
        "200":
          description: "Answered — declared true or false, with the reason"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReleasePhaseResponse"
        "400":
          description: Missing or blank rev
        "503":
          description: "Not answered — the repository is in no catalogue here, or\
            \ the slot file or its archetype could not be read. Retry."
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Repository Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs:
    get:
      summary: "List a repository's CI runs, newest first"
      parameters:
      - description: Return only the newest n runs; omit for all of them
        schema:
          type: integer
          minimum: 1
        name: limit
        in: query
      - description: The repository whose runs to list — required
        required: true
        name: repositoryId
        in: query
        schema:
          type: string
      responses:
        "200":
          description: "The repository's runs with their step boundaries, without\
            \ step output"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRunsResponse"
        "400":
          description: "The repository id is missing or invalid, or the limit is not\
            \ a positive integer"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs/active:
    get:
      summary: "Every queued or running CI run, all repositories, newest first"
      responses:
        "200":
          description: "The active runs with their step boundaries and queue position,\
            \ without step output"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRunsResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs/cancellations:
    post:
      summary: Cancel every unfinished run a repository has for one release request
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelReleaseRequestRunsRequest"
        required: true
      responses:
        "202":
          description: The request's runs have been stopped or asked to stop
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelReleaseRequestRunsResponse"
        "400":
          description: The repository id or the release request id is missing or invalid
        "403":
          description: "The token covers this repository for nobody — no project claim\
            \ and no platform-wide role, or a project claim this instance cannot place\
            \ the repository in"
        "401":
          description: Not Authorized
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /ci/api/runs/finished:
    get:
      summary: "The newest finished CI runs, all repositories, newest first"
      parameters:
      - description: "Return the newest n finished runs; omit for 5, capped at 100"
        schema:
          type: integer
          maximum: 100
          minimum: 1
        name: limit
        in: query
      responses:
        "200":
          description: "The finished runs with their step boundaries, without step\
            \ output"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRunsResponse"
        "400":
          description: The limit is not a positive integer
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs/queue:
    get:
      summary: "The run queue in claim order, with each run's expected start and finish"
      responses:
        "200":
          description: The queue at one instant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QueueResponse"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs/rerun:
    post:
      summary: Run one phase of a release request's CI again
      description: "A release is one pipeline of three phases and a PHASE is a unit\
        \ of work with a state and a rerun. Two of them are runs here — RELEASE_REQUEST\
        \ is phase one, the QA run at release/<id>@mergedSha, and RELEASE is phase\
        \ two, the publish run at <version>@commitSha. Phase three is the deploy and\
        \ is qits-deployments' own release request, so it is not a word this door\
        \ takes. A step inside a run is not a phase, so it cannot be re-fired separately.\
        \ Which phase a run is was decided by the event that triggered it and never\
        \ by which config file produced it, so this addresses a run the same way whatever\
        \ the repository's release.yml composed for it — and the same way for a run\
        \ from a bespoke ci-event-*.yml, which is the escape hatch the grammar still\
        \ has. This decides nothing about the release: the pipeline is the release\
        \ request in qits-projects, the new run announces its verdict on the bus exactly\
        \ as the first one did, and the gate between the two phases goes on holding\
        \ the release where it is until that verdict arrives — a gate delays, it does\
        \ not fail."
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RerunReleaseRequestPhaseRequest"
        required: true
      responses:
        "202":
          description: A new run has been accepted and queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetryRunResponse"
        "400":
          description: "The repository id, the release request id or the phase is\
            \ missing or invalid"
        "403":
          description: "The token covers this repository for nobody — no project claim\
            \ and no platform-wide role, or a project claim this instance cannot place\
            \ the repository in"
        "404":
          description: No such repository
        "409":
          description: "That phase has no run yet, its newest run is still going,\
            \ or it succeeded — a succeeded phase has nothing to ask again"
        "401":
          description: Not Authorized
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
  /ci/api/runs/{runId}:
    get:
      summary: "One CI run with its steps, output and — while it runs — its live step"
      parameters:
      - name: runId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: The run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CiRunDto"
        "404":
          description: No such run
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
        - qits:system
        - qits:agent
  /ci/api/runs/{runId}/cancel:
    post:
      summary: Cancel a queued or running CI run
      parameters:
      - name: runId
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Optional human-readable cancellation reason
        content:
          '*/*':
            schema:
              $ref: "#/components/schemas/CancelRunRequest"
        required: false
      responses:
        "202":
          description: The run has been stopped or asked to stop
        "404":
          description: No such run
        "409":
          description: "The run has already finished, so there is nothing to stop"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
        "400":
          description: Bad Request
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
  /ci/api/runs/{runId}/retry:
    post:
      summary: "Run a finished CI run's pipeline again, at the same commit"
      parameters:
      - name: runId
        in: path
        required: true
        schema:
          type: string
      responses:
        "202":
          description: A new run has been accepted and queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetryRunResponse"
        "404":
          description: No such run
        "409":
          description: "The run has not finished, so there is nothing to retry yet"
        "401":
          description: Not Authorized
        "403":
          description: Not Allowed
      tags:
      - Ci Run Controller
      security:
      - SecurityScheme:
        - qits:admin
info:
  title: qits-ci API
  version: 2026.919.82631
